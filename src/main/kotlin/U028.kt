package org.example

//fun sumParts(ls: IntArray): IntArray = IntArray(ls.size + 1) {
//    val res = IntArray(ls.size + 1)
//    for (i in ls.indices.reversed())
//        res[i] = res[i + 1] + ls[i]
//    return res
//}
package org.example

//fun revRot(nums: String, sz: Int): String = nums
//    .chunked(sz)
//    .filter { it.length == sz }
//    .joinToString("") {
//        if (it.sumOf(Char::digitToInt).mod(2) == 0) it.reversed()
//        else it.drop(1) + it.first()
//    }
